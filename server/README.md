# Pollster server

To run:

* npm install
* npm run dev

## Api end points
* POST /poll  

* GET /poll/:key

* POST /poll/:pollId
* DELETE /poll/:pollId

* POST /poll/:pollId/reply
* GET /poll/:pollId/reply